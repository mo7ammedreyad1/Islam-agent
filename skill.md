# skill.md — استخراج ملف هوية `.md` من كود مشهد خارجي

## إمتى تستخدم الملف ده
لما المستخدم يبعتلك **كود مشهد جاهز** (أي شكل — HTML/Canvas، حتى لو مش
بتركيبة مشروعنا، ممكن يكون بمكتبة رسم تانية، أو SVG، أو حتى وصف/صورة لتصميم)
وعاوز هويته البصرية تتحول لملف `.md` جديد جوه `identities/`، يُستخدم مع أي
سورة/محتوى بعد كده. ده مهمة مختلفة تمامًا عن إنتاج فيديو عادي — **متتبعش
خطوات القسم 2 في `AGENTS.md`**، اتبع الخطوات هنا بدل منها.

## الخطوة 1: اقرا الكود المصدر كامل، مرتين لو محتاج
`cat` الملف كامل (لا `grep`، لا `head`). لو الكود طويل ومتقطع في أول قراءة،
كمّل تقرا الباقي — ممنوع تستنتج تصميم من نص جزئي. لو المستخدم بعت الملف كمرفق
مش عن طريق `run_terminal`، اقراه بالكامل قبل أي خطوة تانية.

## الخطوة 2: افهم السلوك البصري، مش بنية الكود
الكود المصدر ممكن يكون:
- بتركيبة مختلفة تمامًا عن `AGENTS.md` (أسماء متغيرات مختلفة، بلا فصل
  "هوية/طبقة تقنية"، بمكتبة تصدير تانية أو بدون تصدير خالص).
- ناقص أو فيه أجزاء غير مكتملة/كود ميت (متغيرات معرّفة ومش مستخدمة، منطق
  متكرر بصيغتين مختلفتين لنفس الغرض).

مهمتك: **تجاهل بنية الكود نفسها، واستخرج القرار البصري النهائي** — لو فيه
كود ميت أو نسختين متعارضتين لنفس الحاجة، حدد أي نسخة هي الفعلية (اللي بتترسم
فعليًا على الشاشة وقت التشغيل)، ووصّف دي بس.

## الخطوة 3: إيه اللي يُستخرج، وإيه اللي يُتجاهل بالكامل
**يُستخرج** (كل قرار بصري/هندسي، بالقيم الدقيقة):
- الأبعاد، ترتيب الطبقات، كل إحداثي وحجم ومسافة.
- الألوان (قيم hex/rgba دقيقة)، الخطوط (الاسم، الوزن، الحجم، الشرط).
- الحركة: كل صيغة رياضية بالضبط (مش "حركة سلسة"، لازم الرقم والمعادلة
  نفسها لو موجودة في الكود).
- الأصول المطلوبة (صوت/صورة/فيديو): مصدرها، نمط الرابط، طريقة التقطيع/الجلب.
- شكل بيانات المحتوى (أسماء الحقول العامة المطلوبة لأي محتوى مستقبلي).

**يُتجاهل بالكامل** (ممنوع يدخل ملف الهوية):
- أي كود تصدير/رندر (Mediabunny أو أي مكتبة تصدير فيديو تانية) — عندنا
  طبقة تقنية ثابتة جاهزة في `AGENTS.md`، مش محتاجين بتاعة الكود المصدر.
- أي منطق HUD/أزرار/console — ده كله موجود جاهز في الطبقة التقنية الثابتة.
- الكود نفسه بصيغته البرمجية — الناتج وصف كلامي/رقمي، مش كود JS/CSS.

## الخطوة 4: قاعدة الدقة — صارمة جدًا، بدون تقريب
كل رقم وقيمة في ملف الهوية الناتج لازم تكون **مطابقة حرفيًا** لما هو موجود في
الكود المصدر — مش "تقريبًا 40px" لو الكود بيقول `40`، ومش "لون بني" لو الكود
بيقول `#2C1D11`. لو حاجة غامضة أو محتاجة حساب (زي `startY = centerY - totalHeight/2`)،
وصّفها بالمعادلة نفسها، مش بنتيجة رقمية واحدة قد تتغير حسب طول النص.

## الخطوة 5: افصل التصميم العام عن أي محتوى محدد موجود في الكود المصدر
لو الكود المصدر فيه محتوى معين مكتوب جواه (نص سورة بعينها، أسئلة محددة، بيانات
حقيقية مهما كانت) — **ده لا يدخل ملف الهوية خالص**. بس الشكل العام القابل
لإعادة الاستخدام مع أي محتوى مستقبلي يدخل بند "شكل بيانات المحتوى"، بأسماء
حقول عامة + مثال توضيحي واحد بس (زي ما حصل مع تحويل ملف اختبار سورة الشرح
إلى `quran-quiz.md` — الأسئلة نفسها اتشالت، الشكل العام فضل).

## الخطوة 6: الشكل النهائي — نفس القالب بالضبط
افتح `identities/Quran.md` أو `identities/ofoq-style.md` أو
`identities/quran-quiz.md` كمرجع مباشر للتنسيق، واكتب بنفس التسع أقسام
بالترتيب: نظرة عامة، الهندسة والأبعاد، لوحة الألوان (جدول)، الطباعة، الحركة،
الخلفية والتأثيرات، الأصول المطلوبة، شكل بيانات المحتوى، ملاحظات معروفة.

## الخطوة 7: الحفظ
اختار اسم مناسب ومختصر يعكس طابع الهوية (عربي أو إنجليزي، زي الملفات
الموجودة)، واكتب الملف مباشرة بأمر:
```
cat > identities/<الاسم>.md << 'EOF'
...
EOF
```
لو المستخدم لم يحدد اسمًا، اقترح اسمًا واحدًا واستخدمه — لا تسأل إلا لو
التصميم فيه غموض حقيقي يمنعك من الاستخراج بدقة.
