---
name: identity-extractor
description: استخدم الـ Skill دي لما حد يديك ملف HTML لمشهد فيديو (scene/reference file) — من أي مصدر، متوافق مع الرانر أو لأ — والمطلوب منك تستخرج منه هوية بصرية واحدة (محتوى + أصول + تصميم canvas) كملف identities/<اسم>.md احترافي وself-contained، من غير أي كود تنفيذي/تصدير. لا تستخدمها لو المطلوب تنفيذ الفيديو فعليًا أو رندره — دي بس لاستخراج/توثيق الهوية.
license: Internal use
---

# استخراج هوية بصرية من ملف HTML مرجعي

## الهدف من الـ Skill دي
بتاخد أي ملف `.html` (سواء مكتوب أصلًا لمشروعك، أو ملف من مصدر خارجي — AI
Studio، مصمم، مشروع تاني) ويوصف مشهد فيديو بصريًا، وتحوّله لملف
**`identities/<اسم>.md` واحد self-contained**: مواصفة كاملة قابلة للنسخ
الحرفي (وصف + خطوط + CSS + منطقة محتوى + كود جلب أصول + كود رسم)، **بدون أي
حرف من منطق التصدير/الرندر/الأتمتة** — ده بيفضل مسؤولية العقد التقني الثابت
بتاع المشروع المستهدف، مش جزء من الهوية.

**مبدأ العمل الأساسي، ولا يتفاوض عليه**: افصل بين "شكل الفيديو" (تصميم، ألوان،
حركة، تخطيط) و"إزاي بيتصدّر الفيديو" (رندر، ترميز، hooks أتمتة). الأول
بيتُستخرج ويُحفظ. الثاني بيتجاهل تمامًا مهما كان موجود في الملف الأصلي، لأن
نسخه بالحرف هو أكتر سبب شائع لفشل التوافق مع أي رانر آخر.

## خطوة 0: افهم العقد التقني للمشروع المستهدف الأول
قبل ما تستخرج أي حاجة، لازم تعرف شكل "عقد ملف الهوية" المطلوب في المشروع اللي
هتحط فيه الناتج — الأسماء والدوال الإلزامية بتختلف من مشروع لمشروع. لو المشروع
فيه ملف `AGENTS.md` (أو أي دليل مشابه)، افتحه وابحث عن قسم زي "عقد ملف هوية"
أو "identity contract". لو مفيش، افتراضيًا (ومطابق لمشروع القرآن الكريم اللي
اتبنت عليه الـ Skill دي) العقد المطلوب هو:

- ملف الهوية لازم يعرّف: `CONFIG` (كائن فيه `fps`/`width`/`height`/`duration`)،
  `OUTPUT_FILENAME` (نص)، `audioBuffer` (`AudioBuffer` أو `null`).
- ملف الهوية لازم يعرّف دالتين `async`: `prepareIdentity()` (بتجيب/تجهّز أي
  أصول، بتتنادى مرة واحدة قبل أول رسمة) و`drawSceneAtTime(time)` (بترسم فريم
  واحد في وقت معيّن، بتتنادى كل فريم).
- أي أدوات عامة جاهزة (تخطيط نص، easing، إلخ) بيوفرها العقد التقني تلقائيًا —
  ملف الهوية يقدر يستخدمها من غير ما يعيد تعريفها، أو يعرّف بديل لو محتاج.

لو العقد مختلف في مشروعك (أسماء تانية، بنية تانية)، عدّل "قالب الإخراج" تحت
ليطابقه — الجوهر (الفصل بين الهوية والتنفيذ) بيفضل زي ما هو.

## الخطوة 1: فحص التوافق — طبّقه بند بند على الملف المُدخل
افتح الملف وابحث فعليًا (مش انطباع عام) عن كل بند:

1. **الـ render hooks كاملة وشغّالة** (`renderStatus`, `renderProgress`,
   `startVideoRender()`, دعم `?autorender=true`, حدث `video-render-complete`,
   `__renderFilename`/`__renderBase64`).
2. **محرك فيديو معروف ومتوافق** (Mediabunny في المشروع المستهدف — مش
   `ffmpeg`/`MediaRecorder`/`canvas.captureStream`).
3. **AAC encoder polyfill** لو فيه صوت.
4. **أي أصل خارجي يتجاب بـ `fetch()` وقت التشغيل**، مش مسار محلي مُشار له.
5. **التوقيت محسوب من الأصل الحقيقي** (مدة صوت فعلية بعد فك التشفير)، مش
   أرقام ثواني مكتوبة يدويًا.
6. **الصور مرسومة بالكانفاس أو متجابة بـ `crossOrigin="anonymous"`**.

**النتيجة بتحدد مستوى الثقة بس، مش القرار**: سواء عدّى الفحص أو فشل، **الناتج
دايمًا استخراج، أبدًا نسخ حرفي لأي كود تنفيذي**. لو عدّى الفحص بالكامل، الفصل
غالبًا هيكون مباشر (بنية الملف منظّمة أصلًا). لو فشل بند أو أكتر، خد وقتك أكتر
— قرارات تنفيذية (توقيت، مصدر أصل) بتتسرب أحيانًا جوه دوال شكلها "رسم بحت".

## الخطوة 2: صنّف شكل المحتوى والأصول (من غير افتراضات)
اسأل نفسك، وأنت بتقرأ الملف:
- **المحتوى نصّي؟** لو أيوه، إيه شكله بالظبط؟ (آية بس؟ آية+تفسير؟ حديث؟ نص
  حر؟) وثّق الحقول بالضبط زي ما هي، متخترعش حقول إضافية الملف الأصلي مالوش
  ليها أي أثر.
- **فيه صوت؟** من مصدر إيه بالظبط (رابط/نمط الرابط الحقيقي في الملف، حتى لو
  هتستبدله بمصدر تاني لاحقًا لأسباب توافق)؟
- **فيه صورة/فيديو خلفية؟** إلزامي ولا اختياري في التصميم الأصلي؟ فيه fallback
  لو فشل التحميل؟
- **مفيش نص/صوت خالص** (B-roll بصري بحت، أو موشن جرافيكس)؟ وثّق ده صراحة —
  ملف الهوية الناتج هيبقى `audioBuffer = null` ومنطقة محتوى بسيطة أو فاضية.

## الخطوة 3: استخرج — بدقة حرفية، من غير تجميل أو اختراع
**الدرس الأهم في الـ Skill دي كلها**: استخرج **القيم الحقيقية بالحرف** من
الملف — كل لون (hex/rgba)، كل رقم (أحجام خط، مسافات، مدد حركة)، كل اسم خط —
**متخترعش ولا "تحسّن" ولا "تكمّل بالحدس"**. لو الملف الأصلي خلفيته فاتحة
والغمقان مصدره overlay شفاف بس، متكتبش إن الخلفية داكنة لأن "الإحساس العام
داكن" — الفرق ده بيغيّر طريقة الاستخدام الفعلية للملف الناتج بالكامل. لو مش
متأكد من قيمة معيّنة، افتح الملف الأصلي تاني وتأكد بدل ما تخمّن.

**بيتُستخرج** (ينقل لملف الهوية الجديد):
- الألوان، الخطوط (أسماء + أوزان + fallback stacks)، الأبعاد ونسبتها.
- خوارزميات التخطيط/الحركة (إعادة تنفيذها بنفس الأرقام والمنطق بالحرف).
- بنية CSS الخاصة بالواجهة (HUD/أزرار) لو مختلفة عن الافتراضي.
- منطق جلب الأصول **كمفهوم وخطوات** (مش الكود التنفيذي الخام لو بييعتمد على
  hooks/بنية غير متوافقة) — يُعاد كتابته نضيف داخل `prepareIdentity()` بما
  يطابق عقد المشروع المستهدف (خطوة 0)، مع الحفاظ على نفس الفكرة (صورة بحركة
  زووم، مثلًا) وليس بالضرورة نفس السطور.

**بيُستبعد تمامًا** (يتوثّق في "ملاحظات معروفة" مع السبب، ومايتكتبش كوده):
- أي دالة رندر/تصدير/ترميز فيديو.
- أي منطق فحص/اختيار كودك مسبق (زي "جرّب الكودك ده الأول واشوف لو شغال").
- أي render hooks أو جسور أتمتة من الملف الأصلي (هتيجي من العقد التقني
  المستهدف، مش من هنا).
- مصادر أصول غير متوافقة مع سياسة المشروع المستهدف (مثلًا مصدر صوت مختلف عن
  اللي المشروع بيعتمده) — وثّق المصدر الأصلي كمرجع، واستخدم البديل المعتمد.
- أي مكتبة/import محمّلة في الملف الأصلي بس **مالهاش أي استخدام فعلي** (كود
  ميت) — تأكد بالبحث الفعلي عن استدعاءات ليها قبل ما تقرر إنها ميتة، مش
  افتراض من مجرد وجود `<script src>`.
- أي عناصر UX مرتبطة ببنية HTML الملف الأصلي بالذات (زي تعطيل زرار لحد ما
  أصل يتحمّل) لو مش متوافقة مع هيكل الـ DOM الثابت للمشروع المستهدف.

**صحّح، ولا تنسخ، الأخطاء الحقيقية**: لو لقيت باگ واضح في الملف الأصلي (زي
خاصية CSS متكررة بقيمتين متناقضتين، أو منطق فيه race condition واضح)، صححه في
الناتج ووثّق التصحيح صراحة — الهدف هوية شغالة صح، مش نسخة طبق الأصل من عيوب
غير مقصودة.

## الخطوة 4: ابنِ ملف الهوية — القالب الإلزامي
اكتب `identities/<اسم-وصفي>.md` بالأقسام دي بالترتيب:

1. **عنوان + فقرة وصف**: الاسم، الطابع البصري، الأبعاد ونسبتها، حالة
   الاستخدام، **شكل المحتوى والأصول بالتحديد** (زي الخطوة 2). لو الهوية مبنية
   من ملف خارجي، ضيف فقرة "نشأة الهوية" بمصدر الملف واسمه ونتيجة فحص التوافق
   (الخطوة 1) باختصار.
2. **روابط الخطوط** (كتلة `html` جاهزة للنسخ، لو فيه نص).
3. **كتلة CSS كاملة** (كتلة `css` جاهزة، تستهدف الـ IDs/classes الثابتة
   للمشروع المستهدف).
4. **أبعاد الكانفاس** (كتلة `html` بـ`<canvas>`).
5. **كود JS الهوية بالكامل** (كتلة `js` واحدة جاهزة للنسخ الحرفي)، فيها
   بالترتيب: منطقة محتوى معلّمة بتعليق واضح (`⬛ منطقة قابلة للتعديل`)، أي
   ثوابت تصميم، دوال جلب الأصول، دوال الرسم المساعدة، وفي الآخر الدالتين
   الإلزاميتين حسب عقد المشروع المستهدف (خطوة 0).
6. **ملاحظات معروفة**: كل حاجة اتستبعدت مع السبب، أي تصحيح باگ حقيقي عملته،
   وأي قيود/افتراضات (زي "الصورة الاختيارية معطّلة افتراضيًا").

## الخطوة 5: تحقق وظيفي — إلزامي قبل ما تعتبر الملف جاهز
منتجاش الملف من غير ما تتأكد إنه فعلًا هيشتغل لما يتدمج مع العقد التقني
المستهدف:

1. **استخرج** كود JS بتاع الهوية من الملف الناتج برمجيًا (مش نسخ يدوي من
   العين) — أي تعارض بين الاستخراج والنص المكتوب فعليًا سيبان فورًا.
2. **جمّعه** مع الملحق التقني الثابت للمشروع المستهدف (استخرجه هو كمان
   برمجيًا من مصدره) في ملف JS واحد.
3. **شغّله في بيئة معزولة** (Node + `vm` مثلًا) مع mock بسيط لـ `document`،
   `fetch`، `AudioContext`/فك تشفير الصوت، وسياق كانفاس 2D يسجّل النداءات —
   نادِ `prepareIdentity()` ثم `drawSceneAtTime(time)` عند كذا وقت مختلف
   (البداية، النص، قرب النهاية) وتأكد: مفيش أي exception، وفيه رسم فعلي
   (`fillText`/`drawImage` بيتنادوا) في كل فريم مش صفر.
4. **فحص syntax حقيقي على الملف الكامل المُجمّع** (لو الكود بيستخدم
   `import`، استخدم امتداد `.mjs` أو بيئة تدعم ESM فعليًا وقت الفحص — بعض
   أدوات فحص الـ syntax بتتجاهل أخطاء حقيقية بصمت لو الملف `.js` عادي وفيه
   `import`، فتأكد إن الفحص بتاعك حقيقي بتجربته على كود مكسور عمدًا الأول).
5. لو أي خطوة فشلت، ارجع اصلح الملف — **متسلّمش الملف من غير الخطوات دي**.

## خلاصة سريعة (Checklist قبل التسليم)
- [ ] فحصت التوافق (6 بنود) ووثّقت النتيجة.
- [ ] استخرجت القيم الحقيقية بالحرف (ألوان/أرقام/خطوط) — مفيش قيمة مخترعة.
- [ ] استبعدت كل منطق render/hooks/تصدير/فحص كودك.
- [ ] وثّقت كل استبعاد وكل تصحيح باگ في "ملاحظات معروفة".
- [ ] الملف مطابق للقالب الإلزامي (6 أقسام بالترتيب).
- [ ] شغّلت تحقق وظيفي حقيقي (مو نظري) وعدّى من غير أخطاء.
